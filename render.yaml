services:
  - type: web
    name: umpire-analysis
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app.server"
    envVars:
      - key: DATABASE_URL
        sync: false   # You will set this in the Render dashboard
